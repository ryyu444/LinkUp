// Long preview for Sessions found in Dashboard, My Sessions
/*
    When hovered, show the blue edit symbol
    If the blue edit symbol is clicked, check to see if the session was created by the current user
        - if it is, show the delete session and cancel + save changes -> make changes to firebase if delete/save
        - if not, show leave session -> if clicked, remove the user from the current session & save
*/
export default function SessionPreview() {
    return <div>available session</div>;
}